<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>New Recipe</title>
  </head>
  <%- include("Partials/header.ejs") %>
  <body class="min-h-screen">
    <section class="py-12 px-6 bg-white">
      <div class="max-w-md mx-auto bg-gray-100 p-6 rounded shadow-lg">
        <h2 class="text-2xl font-bold text-emerald-700 mb-4">
          Write a new Recipe
        </h2>
        <form action="/addBlog" method="POST">
          <div class="mb-4">
            <label for="title" class="block text-sm font-medium text-gray-700"
              >Recipe Name</label
            >
            <input
              type="text"
              id="recipename"
              name="recipename"
              class="w-full px-3 py-2 border border-gray-300 rounded"
              required
            />
          </div>
          <div class="mb-4">
            <label for="imgurl" class="block text-sm font-medium text-gray-700"
              >Image Link</label
            >
            <input
              type="url"
              id="imgurl"
              name="imgurl"
              class="w-full px-3 py-2 border border-gray-300 rounded"
              required
            />
          </div>
          <div class="mb-4">
            <label for="content" class="block text-sm font-medium text-gray-700"
              >Recipe</label
            >
            <textarea
              id="recipe"
              name="recipe"
              class="w-full px-3 py-2 border border-gray-300 rounded"
              rows="4"
              required
            ></textarea>
          </div>
          <button
            type="submit"
            class="bg-emerald-700 text-white px-4 py-2 rounded hover:bg-emerald-800"
          >
            Publish
          </button>
        </form>
      </div>
    </section>
  </body>
  <%- include("Partials/footer.ejs") %>
</html>
