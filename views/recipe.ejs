<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= recipename %></title>
  </head>
  <%- include("Partials/header.ejs") %>
  <body class="min-h-screen bg-white">
    <div class="my-10 ml-5">
      <a
        href="/allBlogs"
        class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-emerald-600 hover:bg-emerald-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-emerald-500"
      >
        &larr; Back to All Recipes
      </a>
    </div>
    <section class="pb-12 px-6 bg-white">
      <div class="max-w-4xl mx-auto">
        <!-- Recipe Image -->
        <div class="relative overflow-hidden rounded-lg shadow-lg mb-6">
          <img
            src="<%= imgurl %>"
            alt="<%= recipename %>"
            class="w-full h-72 object-cover"
          />
        </div>

        <!-- Recipe Details -->
        <div class="bg-white p-6 rounded-lg shadow-lg mb-20">
          <h1 class="text-3xl font-extrabold text-emerald-700 mb-4">
            <%= recipename %>
          </h1>
          <p class="text-gray-700 text-lg"><%= recipe %></p>
        </div>

        <!-- Back Button -->
      </div>
    </section>
  </body>
  <%- include("Partials/footer.ejs") %>
</html>
